import java.lang.Math;

def arcsine(x) {
	return Math.asin(x);
}
	
def arccos(x) {
	return Math.acos(x);
}
	
def cosine(x) {
	return Math.cos(x);
}
	
def power(x, y) {
	return Math.pow(x, y);
}
	
def sine(x) {
	return Math.sin(x);
}
	
def floorValue(x) {
	return Math.floor(x);
}