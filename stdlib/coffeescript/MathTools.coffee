class MathTools

arcsine: (x, y) -> 
    return asin(x, y);

arccos: (x, y) ->
    return acos(x, y);

cosine: (x, y) ->
    return cos(x, y);

power: (x, y) ->
    return pow(x, y);

sine: (x, y) ->
    return sin(x, y);

floorValues: (x, y) ->
    return floor(x, y);